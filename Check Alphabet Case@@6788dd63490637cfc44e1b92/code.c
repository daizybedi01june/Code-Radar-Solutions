#include <stdio.h>

int main(
    char s;
    scanf("%c",&s);
    if (isupper(s)){
        printf("Uppercase");
    }
    else if (islower(s)){
        printf("Lowercase");
    }
    else{
        printf("Not an alphabet");
    }
    return 0;
)