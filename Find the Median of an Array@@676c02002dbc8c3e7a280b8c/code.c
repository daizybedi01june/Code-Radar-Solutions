#include <stdio.h>

int main(){
    int n;
    scanf("%d",&n);
    for (int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    int sum=0;
    for (int=0;i<n;i++){
        sum+=arr[i];
    }
    int result=(sum/n);
    printf("%d",result);
}